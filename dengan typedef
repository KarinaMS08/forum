#include <stdio.h>
#include <string.h>

typedef struct {
    char nama[30];
    int umur;
} Mahasiswa;

int main() {
    Mahasiswa mhs1;

    strcpy(mhs1.nama, "Manusia A");
    mhs1.umur = 25;

    printf("Nama: %s\n", mhs1.nama);
    printf("Umur: %d\n", mhs1.umur);

    return 0;
}
